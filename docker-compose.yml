services:
  production:
    image: 'ghcr.io/presenton/presenton:latest'
    container_name: presenton_app
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      # Sol taraf: Sunucu (Host) portu, Sağ taraf: Konteyner içindeki port
      - '8877:8877'
    volumes:
      - './app_data:/app_data'
    restart: always
    environment:
      - 'LLM=${LLM:-openai}'
      - 'CAN_CHANGE_KEYS=${CAN_CHANGE_KEYS:-true}'
      - 'OPENAI_API_KEY=${OPENAI_API_KEY}'
      - 'OPENAI_MODEL=${OPENAI_MODEL}'
      - 'GOOGLE_API_KEY=${GOOGLE_API_KEY}'
      - 'GOOGLE_MODEL=${GOOGLE_MODEL}'
      - 'ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}'
      - 'ANTHROPIC_MODEL=${ANTHROPIC_MODEL}'
      - 'CUSTOM_LLM_URL=${CUSTOM_LLM_URL}'
      - 'CUSTOM_LLM_API_KEY=${CUSTOM_LLM_API_KEY}'
      - 'CUSTOM_MODEL=${CUSTOM_MODEL}'
      - 'PEXELS_API_KEY=${PEXELS_API_KEY}'
      - 'EXTENDED_REASONING=${EXTENDED_REASONING}'
      - 'TOOL_CALLS=${TOOL_CALLS}'
      - 'DISABLE_THINKING=${DISABLE_THINKING}'
      - 'WEB_GROUNDING=${WEB_GROUNDING}'
      - 'DATABASE_URL=${DATABASE_URL}'
      - 'DISABLE_ANONYMOUS_TRACKING=${DISABLE_ANONYMOUS_TRACKING}'
      - 'COMFYUI_URL=${COMFYUI_URL}'
      - 'COMFYUI_WORKFLOW=${COMFYUI_WORKFLOW}'
      
      # Port Ayarları:
      - 'PORT=8877'          # Uygulamanın 8877'de çalışmasını sağlar
      - 'HOSTNAME=0.0.0.0'   # Dış bağlantılara izin verir
